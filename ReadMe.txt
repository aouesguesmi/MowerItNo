The input should validate the following constraints:
The user enters either:
- One single line containing the absolute path of the file containing the mowers data.
- Several lines containing directly the data input of the mowers.
To exit the console input, the user should hit Enter twice. The program should then start calculating the new positions of the mowers.